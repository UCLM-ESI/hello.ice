
- Se requiere Ice v3.0


USO
---
   // En un terminal
   $ icebox --Ice.Config=icebox.config

   // En otro terminal.
   // Creamos el topic PrinterTopic y ejecutamos el Subscriber.
   $ icestormadmin --Ice.Config=icestorm.config -e "create PrinterTopic"
   $ ./subscriber

   // En un terminal diferente ejecutamos el publisher
   $ ./publisher
