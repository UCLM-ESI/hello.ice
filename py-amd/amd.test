TestBG(
    './server.py --Ice.Config=server.config',
    shell = True,
    cwd = '$testdir',
    stdout = '$testdir/server.proxy')

Test(
    './client.py "$(head -1 server.proxy)" 4',
    shell = True,
    cwd = '$testdir',
    pre = Poll(FileContains('math1', '$testdir/server.proxy')),
    post = StdOutContains('24'))
